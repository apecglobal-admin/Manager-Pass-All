# Capacitor Android Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an Android-first Capacitor target for ApecGlobal Manager without breaking the existing Node/Electron runtime.

**Architecture:** Capacitor will package the existing `public/` web assets into an Android WebView. Electron and local browser usage keep relative URLs, while Capacitor can use a configured remote API origin through `window.APECGLOBAL_CONFIG.apiBaseUrl` because the Node server is not embedded in the mobile app.

**Tech Stack:** Node.js test runner, plain browser JavaScript, Capacitor v8, Android Gradle project generated by Capacitor.

---

### Task 1: Mobile API Base Configuration

**Files:**
- Modify: `tests/browser-storage-policy.test.js`
- Modify: `src/config.js`
- Modify: `src/server.js`
- Modify: `public/app.js`
- Modify: `public/supabase-client.js`
- Create: `public/config.js`

- [ ] **Step 1: Write failing tests**

Add tests that require:
- public runtime config exposes `apiBaseUrl`
- frontend `api()` routes requests through `apiUrl(path)`
- Supabase vendor bundle loads from the API origin when configured
- static `public/config.js` exists for Capacitor packaging

- [ ] **Step 2: Verify tests fail**

Run: `npm.cmd test -- tests/browser-storage-policy.test.js`
Expected: FAIL because `apiBaseUrl`, `apiUrl`, and static `public/config.js` are not implemented yet.

- [ ] **Step 3: Implement minimal runtime config**

Add `APP_API_BASE_URL`, include `apiBaseUrl` in `getPublicSupabaseConfig()`, serve it from `/config.js`, and create a static fallback `public/config.js`.

- [ ] **Step 4: Implement frontend URL resolution**

Add `runtimeConfig`, `apiUrl(path)`, update `api(path)` to fetch `apiUrl(path)`, and update Supabase library loading to use the configured API origin for `/vendor/supabase.js`.

- [ ] **Step 5: Verify task**

Run: `npm.cmd test -- tests/browser-storage-policy.test.js`
Expected: PASS.

### Task 2: Capacitor Android Scaffold

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Create: `capacitor.config.json`
- Create: `android/**`

- [ ] **Step 1: Install Capacitor packages**

Run: `npm.cmd install @capacitor/core@latest @capacitor/android@latest && npm.cmd install -D @capacitor/cli@latest`
Expected: dependencies are added and lockfile updates.

- [ ] **Step 2: Add npm scripts**

Add scripts:
- `cap:sync`: `cap sync`
- `android:add`: `cap add android`
- `android:sync`: `cap sync android`
- `android:open`: `cap open android`
- `android:run`: `cap run android`

- [ ] **Step 3: Create Capacitor config**

Create `capacitor.config.json` with `appId` `com.apecglobal.manager`, `appName` `ApecGlobal Manager`, `webDir` `public`, and Android scheme `https`.

- [ ] **Step 4: Generate Android project**

Run: `npx.cmd cap add android`
Expected: `android/` is created.

- [ ] **Step 5: Sync web assets**

Run: `npx.cmd cap sync android`
Expected: Capacitor copies `public/` into Android assets and updates native dependencies.

### Task 3: Verification

**Files:**
- Inspect: all changed files

- [ ] **Step 1: Run full tests**

Run: `npm.cmd test`
Expected: all tests pass.

- [ ] **Step 2: Run Capacitor sync**

Run: `npm.cmd run android:sync`
Expected: sync exits with code 0.

- [ ] **Step 3: Inspect git status**

Run: `git status --short`
Expected: only Capacitor/runtime-config related files changed.
